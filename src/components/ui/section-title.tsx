const SectionTitle = ({ title }: { title: string }) => {
  return (
    <div className="max-w-7xl px-2 my-5">
      <h2 className="font-bold text-2xl">{title}</h2>
    </div>
  );
};

export default SectionTitle;
